🧠 FSN GLOBAL COUNT + ICON REDESIGN – FOR REPLIT AI
✅ OBJECTIVE
Replace the current static "🌐 8,421 FSN names claimed globally" text and the emoji next to "Level 1 – Explorer" with:

A live, animated flip clock-style counter that shows the number of .fsn names claimed globally.

A custom-styled level badge icon, not using an emoji, with a glowing, futuristic look.

These changes must match the existing neon cyber style already used on the FSN dashboard and homepage.

✅ PART 1 – REPLACE STATIC TEXT WITH NEON FLIP CLOCK COUNTER
🔧 FUNCTIONAL OVERVIEW
You will:

Create a custom flip-style counter (numbers flip as they increment).

Set an initial value (e.g., 8421) and simulate steady growth (e.g., +1 every 7 seconds).

Display the counter in a glowing neon clock frame.

Label it below as: "FSN names claimed globally".

🔧 STEP-BY-STEP INSTRUCTIONS
STEP 1: REMOVE OLD TEXT
Open the Dashboard component file, likely located in:

pages/dashboard.jsx or components/Dashboard.jsx.

Locate this line:

jsx
Copy
Edit
<div className="social-proof">🌍 8,421 FSN names claimed globally</div>
DELETE that entire div.

STEP 2: INSTALL FLIP CLOCK PACKAGE (OPTIONAL)
If using React, install a flip number library:

bash
Copy
Edit
npm install react-flip-numbers
If not allowed, skip this and implement manually with styled spans.

STEP 3: ADD THIS COUNTER TO DASHBOARD JSX
jsx
Copy
Edit
import FlipNumbers from 'react-flip-numbers';

<div className="fsn-global-counter">
  <FlipNumbers
    height={32}
    width={24}
    color="#00FFFF"
    background="transparent"
    play
    perspective={100}
    numbers={fsnCount}
  />
  <p className="fsn-counter-label">FSN names claimed globally</p>
</div>
STEP 4: ADD THIS STATE LOGIC ABOVE RENDER
Put inside the component:

jsx
Copy
Edit
const [fsnCount, setFsnCount] = useState("8421");

useEffect(() => {
  const interval = setInterval(() => {
    setFsnCount(prev => {
      const newVal = String(parseInt(prev) + 1);
      return newVal;
    });
  }, 7000); // +1 every 7 seconds
  return () => clearInterval(interval);
}, []);
STEP 5: STYLE THE COUNTER + LABEL
Paste into your global or dashboard CSS:

css
Copy
Edit
.fsn-global-counter {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  margin-bottom: 20px;
}

.fsn-counter-label {
  margin-top: 8px;
  font-size: 1rem;
  color: #00FFFF;
  font-weight: 600;
  text-shadow: 0 0 8px rgba(0, 255, 255, 0.5);
  letter-spacing: 0.5px;
}
STEP 6: ERROR CHECKING
✅ Confirm the counter appears above the XP/Signal/Connection boxes.
✅ Confirm the digits flip cleanly, and digits do not break layout.
✅ Counter should not pause or freeze — must tick every 7 seconds.
✅ Confirm text stays centered and doesn't wrap badly on mobile.

✅ PART 2 – REMOVE EMOJIS & ADD CUSTOM GLOW BADGE ICON
🔧 STEP-BY-STEP INSTRUCTIONS
STEP 1: LOCATE CURRENT BADGE CODE
Find this:

jsx
Copy
Edit
<div className="user-badge">🧭 Level 1 – Explorer</div>
Delete the emoji part inside the tag. Replace it with a stylized <div> block to act as a custom placeholder icon.

STEP 2: INSERT THIS JSX INSTEAD:
jsx
Copy
Edit
<div className="user-badge">
  <div className="badge-icon-placeholder"></div>
  Level 1 – Explorer
</div>
STEP 3: ADD THIS CSS TO STYLE THE CUSTOM ICON
css
Copy
Edit
.badge-icon-placeholder {
  width: 18px;
  height: 18px;
  margin-right: 8px;
  display: inline-block;
  border-radius: 50%;
  background: radial-gradient(circle at center, #00FFFF, #003E47);
  box-shadow: 0 0 6px #00FFFF, 0 0 12px #00FFFF, inset 0 0 4px #003E47;
  vertical-align: middle;
}
Also ensure .user-badge uses:

css
Copy
Edit
.user-badge {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 10px 18px;
  border: 1px solid #00FFFF;
  border-radius: 20px;
  font-weight: bold;
  background: #0a0a0a;
  animation: glow 1.5s ease-in-out infinite alternate;
}
✅ ERROR CHECKING FOR BADGE
✅ Make sure the placeholder icon renders with a glowing circle.
✅ Text and icon are horizontally aligned (not stacked).
✅ Confirm no emoji is rendered anywhere in .user-badge.

✅ SUMMARY CHECKLIST
Element	Pass Condition
Flip counter works	Numbers flip every 7 seconds, starts at 8421, looks neon and cyberpunk
No emoji remains	All emoji symbols removed from dashboard and replaced with stylized elements
Glowing badge icon	Custom round placeholder icon with glow effect replaces emoji
No layout break	All elements scale properly on mobile, tablet, and desktop
FSN count updates live	Global FSN count increases dynamically, without page reload