🚀 Project Goal
Create a full-screen landing page for FreeSpace Network that visually matches the uploaded image (Homepage.png). It should look like a professional crypto Web3 onboarding interface, with glowing neon UI, balanced layout, and responsive styling.

📐 Layout Instructions
General Layout
Full-bleed page: width: 100vw, height: 100vh

Use a main container with display: flex; align-items: center; justify-content: space-between;

Add padding: 0 5vw; and gap: 4vw;

HTML Layout
html
Copy
Edit
<body>
  <section class="hero">
    <div class="hero-content">
      <img src="/assets/fsn-logo.svg" alt="FreeSpace Network Logo" class="logo" />
      <h1>Crypto starts with your <span class="highlight">.fsn</span> name</h1>
      <p class="subtext">Claim your Web3 identity.<br>Earn XP. Emit your Signal.</p>
      <button class="cta" id="claim-btn">Claim your .fsn name</button>
    </div>
    <div class="hero-graphic">
      <img src="/assets/fsn-hex.png" alt=".fsn Hex" class="fsn-hex" />
    </div>
  </section>
</body>
🎨 CSS Styling Instructions
Font
css
Copy
Edit
@import url('https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700&display=swap');

body {
  margin: 0;
  padding: 0;
  font-family: 'Manrope', sans-serif;
  background: radial-gradient(circle at center, #0a0f1c, #04070b);
  color: #00f0ff;
  overflow-x: hidden;
}
Hero Layout
css
Copy
Edit
.hero {
  display: flex;
  justify-content: space-between;
  align-items: center;
  min-height: 100vh;
  padding: 0 5vw;
  gap: 4vw;
}

.hero-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  max-width: 540px;
}

.hero-graphic {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
}
Logo
css
Copy
Edit
.logo {
  width: 60px;
  filter: drop-shadow(0 0 8px #00f0ff);
  margin-bottom: 1.5rem;
}
Headline
css
Copy
Edit
h1 {
  font-size: clamp(2.5rem, 6vw, 4rem);
  font-weight: 600;
  line-height: 1.2;
  margin-bottom: 1rem;
  text-shadow: 0 0 12px rgba(0, 255, 255, 0.4);
  color: #00f0ff;
}

h1 .highlight {
  font-weight: 700;
  color: #00f0ff;
  text-shadow: 0 0 16px rgba(0, 255, 255, 0.6);
}
Subheadline
css
Copy
Edit
.subtext {
  font-size: 1.25rem;
  font-weight: 400;
  line-height: 1.6;
  max-width: 420px;
  margin-bottom: 2rem;
  color: #c0faff;
}
CTA Button
css
Copy
Edit
.cta {
  padding: 0.75em 2em;
  font-size: 1.1rem;
  color: #00f0ff;
  background: transparent;
  border: 2px solid #00f0ff;
  border-radius: 30px;
  cursor: pointer;
  transition: 0.3s ease;
}

.cta:hover {
  transform: scale(1.03);
  box-shadow: 0 0 12px rgba(0, 255, 255, 0.5);
}
Input Field on Button Click
When button is clicked, replace it with an input:

html
Copy
Edit
<input type="text" class="fsn-input" placeholder="This will be your Signal" />
CSS for input field:

css
Copy
Edit
.fsn-input {
  padding: 0.75em 2em;
  border: 2px solid #00f0ff;
  border-radius: 30px;
  background: transparent;
  color: #00f0ff;
  font-size: 1rem;
  width: 300px;
}

.fsn-input::placeholder {
  color: #00f0ff;
  opacity: 0.6;
  font-style: italic;
}
JavaScript for Input Swap
html
Copy
Edit
<script>
  document.getElementById("claim-btn").addEventListener("click", function() {
    const input = document.createElement("input");
    input.setAttribute("type", "text");
    input.setAttribute("placeholder", "This will be your Signal");
    input.classList.add("fsn-input");
    this.replaceWith(input);
  });
</script>
.fsn Hex Image
css
Copy
Edit
.fsn-hex {
  width: 320px;
  filter: drop-shadow(0 0 12px rgba(0, 255, 255, 0.6));
}
📱 Responsive Media Query
css
Copy
Edit
@media (max-width: 768px) {
  .hero {
    flex-direction: column;
    padding: 2rem;
    text-align: center;
  }

  .hero-content, .hero-graphic {
    width: 100%;
    align-items: center;
  }

  .cta, .fsn-input {
    width: 100%;
    text-align: center;
  }

  .fsn-hex {
    width: 240px;
    margin-top: 2rem;
  }
}
✅ Final Developer Checklist
Match spacing, typography, logo placement to Homepage.png

Use correct glow effects and colors (#00f0ff)

Button should morph to input with placeholder “This will be your Signal”

All elements should scale gracefully under 768px

No uppercase anywhere

Text should be left-aligned on desktop, centered on mobile

